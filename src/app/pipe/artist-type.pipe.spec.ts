import { ArtistTypePipe } from './artist-type.pipe';

describe('ArtistTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistTypePipe();
    expect(pipe).toBeTruthy();
  });
});
